# CATF

Code for paper "Context-Aware Feature Relevance Modeling for Tabular Prediction via Semantic-Conditioned Cross-Modal Fusion"

## Installation

`conda` virtual environment is recommended. 

```
conda create -n catf python=3.10
conda activate catf
pip install -r requirements.txt
```

